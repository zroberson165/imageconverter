# imageconverter
simple script to batch convert images
run as script.py --input /inputpath --output /outputpath --format .typeofformat --quality (default is 75) --resize resizepercentage (optional) --recursive (if you add this it will go through subdirectories and convert)
